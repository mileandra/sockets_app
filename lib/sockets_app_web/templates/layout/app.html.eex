<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>SocketsApp · Phoenix Framework</title>
  </head>
  <body>
    <main role="main" class="container">
      <%= render @view_module, @view_template, assigns %>
    </main>
    <%= if assigns[:user_token] do %>
    <script type="text/javascript">
      window.userToken = '<%= @user_token %>'
    </script>
    <% end %>
    <%= if assigns[:current_user] && @current_user.role == :student do %>
      <script type="text/javascript" src="<%= Routes.static_path(@conn, "/app.js") %>"></script>
    <% end %>
    <%= if assigns[:current_user] && @current_user.role == :teacher do %>
      <script type="text/javascript" src="<%= Routes.static_path(@conn, "/admin.js") %>"></script>
    <% end %>
  </body>
</html>
